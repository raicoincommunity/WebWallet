import { ShortAccountPipe } from './short-account.pipe';

describe('ShortAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
